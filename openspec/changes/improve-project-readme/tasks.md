## 1. 内容事实与状态核验

- [x] 1.1 盘点当前代码、界面和 OpenSpec 变更，建立 README 能力状态表，逐项标记“已实现”“开发中”或“规划中”，并记录每项的代码或变更依据。
- [x] 1.2 从 `package.json`、`src-tauri/Cargo.toml` 和 `src-tauri/tauri.conf.json` 核对 Windows 版本、WebView2、Bun、Rust、开发、检查、测试和构建命令。
- [x] 1.3 核对仓库现有的许可证、发布页、贡献指南和资源授权文件，只保留真实存在的链接；不存在的入口不得在 README 中占位承诺。
- [x] 1.4 明确插件化表述边界：将插件宿主、`.petpack`、安装/启用/禁用和 SDK 标记为开发中或规划中，并链接 `add-plugin-system-foundation`，不描述为当前可用功能。

## 2. README 媒体资产

- [x] 2.1 创建 `docs/assets/readme/`，确定桌宠本体、玻璃快捷面板和完整统计三种稳定截图的文件名、尺寸和中文替代文本。
- [ ] 2.2 使用干净测试数据重新生成桌宠与快捷面板截图，确认画面不包含用户名、绝对路径、私人软件记录、窗口标题、账号或未授权素材。
- [x] 2.3 生成完整统计截图，使用无敏感信息的示例数据，并确认截图内容与当前应用界面一致。
- [x] 2.4 压缩 README 图片并检查清晰度、透明背景和仓库体积；如加入动图，同时保留同场景静态 PNG 回退。

## 3. 根 README 编写

- [x] 3.1 新增中文根级 `README.md`，首屏使用“小桌宠”产品名、一句话定位和真实桌宠媒体，确保桌宠而不是使用统计成为第一视觉信号。
- [x] 3.2 编写“当前可用”章节，按核验结果说明桌宠窗口、点击快捷面板、大小设置、本地使用统计和当前皮肤能力。
- [x] 3.3 编写“可插件化桌宠”章节，加入文本架构图、官方预设插件与用户插件关系，并解释首次仅保留默认皮肤的目标策略。
- [x] 3.4 加入能力状态表和路线图，将当前功能、插件基础设施、未来 SDK、在线目录和逻辑插件按“已实现/开发中/规划中”分类。
- [x] 3.5 编写快速开始和开发章节，提供环境要求、`bun install`、`bun run tauri:dev`、检查、测试和构建命令。
- [x] 3.6 编写隐私章节，准确说明本地存储、统计范围以及不收集窗口标题、文档名、键盘内容、截图、网络活动和遥测的边界。
- [x] 3.7 编写项目结构、OpenSpec 开发方式、资源授权和贡献说明，链接现有文件和变更，不复制详细任务清单或长版权文本。
- [x] 3.8 通读并压缩 README，确保首屏简洁、各章节无重复、没有空徽章、无效链接、夸大表述或不存在的下载入口。

## 4. 自动检查与手工验收

- [x] 4.1 增加轻量 README 自动测试，检查根文件、必需标题、相对媒体文件、仓库内链接、关键开发命令和插件状态标签均存在且有效。
- [ ] 4.2 运行 README 自动测试、`bun run check`、`bun test` 和 `bun run build`，确认文档测试与现有前端检查全部通过。
- [x] 4.3 在 GitHub 兼容的 Markdown 预览中检查桌面和窄窗口排版，确认图片、代码块、状态表、中文文本和相对链接没有溢出或失效。
- [ ] 4.4 在 Windows 10/11 x64 上按 README 执行安装与 `bun run tauri:dev`，确认命令可用、截图对应当前界面且 WebView2/工具链前置条件准确。
- [x] 4.5 对截图和 README 做一次隐私与授权人工检查，确认没有桌面敏感信息、未获授权素材、遥测承诺偏差或重复授权说明。
- [x] 4.6 运行 `openspec validate "improve-project-readme" --strict`，确认纯文档变更保持 `skip_specs: true` 且 proposal、design 和任务清单一致。
