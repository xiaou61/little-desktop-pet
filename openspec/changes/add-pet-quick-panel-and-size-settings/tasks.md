## 1. 大小偏好与可测试几何

- [x] 1.1 定义桌宠大小百分比的规范化规则和版本化 `pet-preferences.json` 数据结构，覆盖缺失、损坏、未知版本、越界和非 10% 步进值的回退测试。
- [x] 1.2 实现大小偏好的原子写入、读取和错误返回，验证临时文件清理、旧的有效文件保留以及 Windows 路径处理。
- [x] 1.3 定义桌宠锚点、显示器工作区和面板尺寸的物理像素模型，实现下方定位、上方翻转、左右 clamp、负坐标显示器和超小工作区的纯函数测试。
- [x] 1.4 定义快捷面板开关、内部点击、外部点击和 `Escape` 的状态转换，测试重复事件、关闭后旧响应和拖动结束不误开面板。

## 2. 原生桌宠缩放

- [x] 2.1 扩展角色帧生成，使 DPI 缩放后再应用 70%–160% 的大小比例，并验证窗口尺寸、预乘 alpha 和命中图在 70%、100% 和 160% 下保持一致。
- [x] 2.2 扩展 `PetHandle` 和原生消息协议，支持读取/设置大小、重建 GDI surface、暂停单帧动画并在失败时保留上一组可用帧。
- [x] 2.3 在大小改变后复用现有工作区 clamp 和位置保存逻辑，验证靠近右下角放大、跨显示器缩放和完整角色可见。
- [x] 2.4 在桌宠启动前加载合法大小偏好，并在成功应用后保存；验证首次启动默认 100%、重启恢复和损坏偏好不阻止桌宠、托盘及采集器启动。
- [x] 2.5 扩展 DPI、显示器布局和桌宠移动通知，向主线程提供包含桌宠矩形、工作区和 DPI 的锚点数据。

## 3. 快捷面板窗口控制器

- [x] 3.1 在 Tauri 生命周期中加入唯一快捷面板标签、打开状态和窗口代数管理，确保面板只按需创建且关闭后不残留输入拦截。
- [x] 3.2 实现面板创建参数、无边框置顶行为、工作区定位和一次性尺寸修正，并为定位控制器接入锚点 DTO 的纯函数结果。
- [x] 3.3 在 Windows 适配层尝试设置 Acrylic/Backdrop，检测失败、透明效果关闭和高对比度状态并切换到高不透明度回退背景。
- [x] 3.4 实现桌宠点击对快捷面板的切换、面板失焦关闭、`Escape` 关闭和面板内部动作保护，验证异步响应不会在关闭后重新显示面板。
- [x] 3.5 实现“查看完整统计”和“设置”的窗口/视图跳转，确保完整统计窗口唯一、快捷面板关闭或失活且后台采集不中断。
- [x] 3.6 处理面板创建、渲染和定位失败，验证桌宠仍可点击拖动、托盘完整统计入口仍可用，并记录可读的本地错误状态。

## 4. 快捷面板与设置前端

- [x] 4.1 新增中文快捷面板视图，调用现有今日汇总接口，展示记录状态、总时长、最多三条排序后的软件摘要、空状态和读取错误状态。
- [x] 4.2 实现“查看完整统计”“设置”、关闭按钮和键盘语义，确保焦点顺序、可见焦点、按钮标签和面板内部点击符合无障碍要求。
- [x] 4.3 实现玻璃材质前景、半透明/不透明回退、边框和轻微打开关闭过渡，覆盖 `prefers-reduced-motion`、减少透明度和窄尺寸下的文字溢出。
- [x] 4.4 在设置视图中实现 70%–160%、10% 步进、默认 100% 的滑块或等价控件，显示当前值、应用状态和保存失败提示。
- [x] 4.5 为快捷面板和大小设置编写 Svelte/TypeScript 测试，覆盖今日摘要合并、空状态、入口动作、合法值、规范化值和错误提示。

## 5. 生命周期与现有功能集成

- [x] 5.1 将桌宠短单击回调接入快捷面板控制器，保留拖动阈值和透明像素穿透，验证拖动结束不打开面板、再次单击可关闭面板。
- [x] 5.2 将大小偏好路径、面板控制器和 `PetHandle` 纳入启动/退出顺序，验证退出先保存统计与桌宠状态，再销毁面板和原生窗口线程。
- [x] 5.3 保持托盘“显示/隐藏桌宠”“打开使用统计”“退出”和重复启动行为，验证快捷面板打开期间托盘操作不会创建重复窗口或采集器。
- [x] 5.4 检查内容安全策略、打包资源和本地接口，确保快捷面板不引入远程字体、网络请求、分析 SDK 或新的隐私字段。

## 6. 自动化与 Windows 验证

- [x] 6.1 运行 Rust 格式化、clippy、单元测试和集成测试，确认偏好、几何、缩放、面板状态和故障回退全部通过。
- [x] 6.2 运行 Bun 前端检查和测试，确认中文面板、设置控件、可访问性属性和减少动态效果样式无类型或渲染错误。
- [ ] 6.3 构建 Windows release 包并手工验证单击桌宠打开玻璃快捷面板、再次单击/外部点击/`Escape` 关闭、面板内部操作和完整统计入口。
- [ ] 6.4 在面板靠近屏幕底部、左右边缘、100%/125%/150% DPI 和多显示器布局下验证默认下方定位、上方翻转、工作区 clamp、拖动和大小改变后的重新定位。
- [ ] 6.5 手工验证 70%/100%/160% 大小调整、重启恢复、损坏偏好回退、透明像素命中和桌宠不抢焦点；记录异常时的回退表现。
- [x] 6.6 在面板关闭的 release 稳定阶段测量五分钟 CPU、私有工作集和 WebView 生命周期，并检查本地文件与运行时网络行为符合隐私和资源预算。
- [x] 6.7 运行 `openspec validate --all --strict`，逐条对照两个新能力的场景和旧桌宠集成场景，保留 change 未归档以待用户验收。
