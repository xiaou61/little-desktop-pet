## 1. 皮肤资源与 Manifest

- [x] 1.1 准备简洁云朵、橙色小龙、三花猫三套透明 RGBA 主资源和小尺寸缩略图；简洁云朵使用经权利人授权的 Cloudling 待机首帧，其他两套为项目自有原创资源，并在资源说明中记录来源、授权和尺寸约束。
- [x] 1.2 新增 Rust 皮肤资源模块和静态 manifest，定义稳定 ID、中文名称、基准逻辑尺寸、主资源与缩略图字节，并提供只返回通过校验条目的清单函数。
- [x] 1.3 为 manifest 增加资源校验：PNG 可解码、RGBA 透明边界、尺寸范围、缩略图存在且 ID 唯一；补充有效、损坏、缺失和不合规资源的 Rust 单元测试。

## 2. 本地偏好与启动恢复

- [x] 2.1 新增 `pet-skin-preferences.json` 的版本 1 读写模块，采用严格 JSON、临时文件、`sync_all`、Windows 原子替换和临时文件清理，并覆盖缺失、损坏、未知版本、未知 ID 和写入失败测试。
- [x] 2.2 将皮肤偏好路径接入 Tauri 启动流程和 `PetHandle`，在首次构建桌宠帧前校验并加载皮肤；无效偏好或资源不可用时使用简洁云朵且不阻止托盘、采集器和统计窗口启动。
- [x] 2.3 增加启动恢复的无 HWND 测试，确认合法皮肤、缺失文件、损坏文件和未知皮肤 ID 的最终选择符合规格，并确认现有 `pet-preferences.json` 大小偏好保持兼容。

## 3. 原生桌宠帧切换

- [x] 3.1 将现有单一 `pet.png` 解码入口和 `FrameSet` 构建流程改为接收 manifest 皮肤源，复用相同的 DPI、大小百分比、待机动作、预乘 BGRA 和 alpha 命中规则。
- [x] 3.2 增加桌宠线程的皮肤切换消息与结果类型：校验 ID、构建候选帧和 GDI surface，全部成功后一次性替换当前状态，并保留原位置和大小。
- [x] 3.3 为切换请求加入串行化或请求代数保护，失败时保留上一套帧；成功后按现有工作区 clamp 规则重新约束桌宠，并通知打开中的快捷面板更新锚点。
- [x] 3.4 补充 Rust 测试，覆盖三套皮肤在 30%–160% 大小、多个 DPI 下的帧/命中图尺寸一致性、工作区边缘约束、重复选择和切换失败回退。

## 4. Tauri 命令与前端数据契约

- [x] 4.1 新增查询皮肤清单、读取当前皮肤和设置皮肤的 Tauri 命令与序列化 DTO，注册到生命周期命令列表；拒绝路径、任意字节、远程 URL 和未知皮肤 ID，并区分资源、帧构建和偏好保存错误。
- [x] 4.2 在 TypeScript 类型和 API 封装中加入皮肤清单、缩略图、当前状态、设置结果和错误反馈模型，确保 API 不触碰统计采集接口或发起网络请求。
- [x] 4.3 为命令输入规范化、成功响应、未知 ID、资源失败和本地保存失败补充自动化测试或纯函数测试。

## 5. 快捷面板皮肤画廊

- [x] 5.1 在 `QuickPanel.svelte` 的桌宠设置视图中加入皮肤画廊，显示三套中文名称、缩略预览、当前选中状态和不可用状态；加载时提供稳定占位，选择时保持大小控制可用。
- [x] 5.2 接入皮肤选择的即时应用、保存结果和失败回退：成功后同步当前标记，失败后保留旧标记并显示中文 `role="alert"`；返回概览、关闭面板和打开完整统计不撤销选择。
- [x] 5.3 为画廊补充 CSS 和交互状态，确保缩略图、焦点环、键盘操作、高对比度、窄窗口和 `prefers-reduced-motion` 下文字与控件不重叠且可读。
- [x] 5.4 扩展 Svelte/Vitest 测试，覆盖默认云朵、三套皮肤展示、离线本地预览、选择成功、重复选择、不可用皮肤、保存失败和面板关闭后的状态保持。

## 6. 集成验证与 Windows 手工验收

- [x] 6.1 运行前端类型检查、构建和测试（`bun run check`、`bun run build`、`bun test`），并运行 Rust 测试（`cargo test --manifest-path src-tauri/Cargo.toml`）；修复与皮肤改动相关的回归。
- [ ] 6.2 在 Windows 10/11 x64 上启动 Tauri 开发或构建版本，验证首次启动默认简洁云朵、三套皮肤点击切换、大小 30%/160%、重启恢复、快捷面板锚定和托盘/统计入口。
- [ ] 6.3 在 Windows 手工验证中覆盖损坏或删除皮肤偏好后的云朵回退、切换资源失败后的旧皮肤保留、DPI/多显示器/屏幕边缘、锁屏或挂起恢复、跨日和关闭快捷面板；记录桌宠待机资源占用与切换时短暂峰值。
- [x] 6.4 运行 `openspec validate "add-pet-skin-customization" --strict`，确认所有规格场景、任务状态、资源授权说明和回滚说明齐全。
