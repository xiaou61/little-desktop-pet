## 1. 角色资产与依赖

- [x] 1.1 生成一个青绿、珊瑚和暖黄色配色的原创圆形机器人桌宠，完成色键去背景并验证 PNG alpha、透明四角和主体覆盖率。
- [x] 1.2 将最终角色保存到 `src-tauri/assets/pet.png`，在 Rust 中通过 `include_bytes!` 打包，并记录可替换资产边界。
- [x] 1.3 为现有 `windows` crate 补充分层窗口、GDI、DPI、显示器和鼠标处理所需的最小 features，并添加只启用 PNG 的 `image` 依赖。

## 2. 可测试的窗口逻辑

- [x] 2.1 定义物理坐标、窗口矩形和显示器工作区模型，编写默认右下角、有效位置恢复、离屏回退和边缘 clamp 的单元测试。
- [x] 2.2 实现 alpha 命中纯函数，测试窗口外、透明像素、半透明阈值和不透明像素。
- [x] 2.3 实现单击/拖动分类状态，使用 Windows 系统拖动阈值，测试短单击、超过阈值拖动和拖动结束不触发单击。
- [x] 2.4 实现版本化 `pet-state.json` 的读取、原子保存和损坏文件回退，并用临时目录测试。

## 3. 原生桌宠窗口

- [x] 3.1 解码内嵌 PNG，按照 DPI 生成预乘 BGRA 帧和 alpha 命中图，并预计算低频呼吸/浮动待机帧。
- [x] 3.2 建立独立 Win32 消息线程，注册窗口类并创建无边框、分层、工具、置顶和不激活的桌宠窗口。
- [x] 3.3 使用 `UpdateLayeredWindow` 绘制当前帧，处理待机定时器、DPI 变化、显示器变化和所有 GDI 资源释放。
- [x] 3.4 处理逐像素 `WM_NCHITTEST`、不抢焦点、鼠标捕获、拖动、工作区约束和拖动结束保存。
- [x] 3.5 实现 `PetHandle` 的显示、隐藏、确保可见、保存位置和干净终止命令，并让初始化失败不会终止采集器或托盘。

## 4. Tauri 生命周期集成

- [x] 4.1 修改启动顺序：先启动采集器，再启动桌宠和托盘；正常启动不再自动打开统计面板。
- [x] 4.2 让桌宠短单击通过 Tauri 主线程打开或关闭唯一快捷面板，快捷面板中的完整统计入口再打开统计页，关闭面板后桌宠继续运行。
- [x] 4.3 将托盘改为动态“显示/隐藏桌宠”“打开使用统计”“退出”，并让左键单击托盘默认显示桌宠。
- [x] 4.4 修改单实例回调，使重复启动显示并修正已有桌宠位置，而不是直接打开统计面板。
- [x] 4.5 把产品名、托盘提示、统计面板标题和前端页面标题统一为中文“小桌宠/使用统计”语义，保留现有统计数据和接口。
- [x] 4.6 在退出路径中先保存桌宠位置和统计缓冲，再关闭原生窗口线程、统计面板、托盘和采集器。

## 5. 验证与交接

- [x] 5.1 运行 Rust 格式化、clippy、单元/集成测试、Svelte 检查、前端测试和 Windows release 构建，解决所有失败。
- [ ] 5.2 在 Windows 实机验证首次启动只显示桌宠、透明背景、始终置顶、跳过任务栏、透明像素穿透和单击不抢当前应用焦点。
- [ ] 5.3 验证单击打开/关闭快捷面板、从面板进入完整统计、拖动不误开面板、关闭统计保留桌宠、托盘显示/隐藏、重复启动和退出保存。
- [ ] 5.4 在 100%、125% 和 150% 缩放及可用的多显示器布局中验证拖动、位置恢复、离屏回退和完整可见。
- [ ] 5.5 截取桌面实机截图并检查角色边缘、尺寸、透明色边、矩形背景、统计面板遮挡和整体桌宠感。
- [x] 5.6 使用 release 构建关闭统计面板后测量五分钟，确认桌宠待机时平均 CPU 低于 1%、私有工作集不超过 50 MiB。
- [x] 5.7 检查本地文件和运行时网络行为，确认只新增角色资产和最终窗口坐标，不扩大隐私采集范围。
- [x] 5.8 运行 `openspec validate --all --strict`，逐条对照 desktop-pet-shell 场景，并在用户接受前保持 change 未归档。
