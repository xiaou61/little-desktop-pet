## 1. 变更准备与协议边界

- [x] 1.1 在开始代码实现前，协调尚未归档的 `add-pet-skin-customization` 变更：将“三套默认可选皮肤”收敛为“默认仅 `simple-cloud`，其他皮肤由插件目录安装”，并验证两份 OpenSpec 不再存在相互矛盾的场景。
- [x] 1.2 在 Rust 侧建立插件领域模块和公开 DTO，定义插件 ID、版本、来源、状态、权限、贡献类型和面向前端的错误码，不让调用方依赖路径或 Win32 细节。
- [x] 1.3 选择并接入受维护的 ZIP 读取依赖，为 `.petpack` 读取定义文件数、压缩展开总量、单文件和图片资源限制；确认新增依赖不会进入桌宠常驻渲染路径。
- [x] 1.4 为合法皮肤包、非法 manifest、越界路径、超限资源、损坏 PNG 和不兼容宿主版本准备可复用测试夹具。

## 2. 插件包与注册表

- [x] 2.1 实现严格的版本化 manifest 解析与校验，覆盖稳定 ID、语义版本、`hostApi`、贡献 ID 命名空间、权限白名单和声明式贡献类型。
- [x] 2.2 实现受限 `.petpack` 读取和资源校验，拒绝绝对路径、路径穿越、重复条目、未知贡献、脚本入口、超限文件和无法解码的图片。
- [x] 2.3 为 manifest 和包校验补充 Rust 单元测试，覆盖合法包、未知字段、重复贡献、越界路径、图片透明边界和资源上限。
- [x] 2.4 实现版本化 `plugin-registry.json` 读写、状态迁移和插件独立设置命名空间，复用严格 JSON、临时文件、`sync_all` 和 Windows 原子替换策略。
- [x] 2.5 为注册表补充单元测试，覆盖首次 bootstrap、缺失/损坏/未知版本恢复、重复状态迁移、重启恢复和命名空间隔离。
- [x] 2.6 实现插件暂存、完整校验、版本目录原子提交和失败清理；安装失败时保留旧版本、旧状态和当前桌宠外观。
- [x] 2.7 实现安装、启用、禁用、卸载和 `broken` 状态转换，保护核心宿主、插件管理器和最后一套可用默认皮肤。
- [x] 2.8 为安装器和状态机补充故障注入测试，覆盖写入失败、注册表失败、启用部分失败、重复启用、禁用后重启和卸载保护。

## 3. 宿主运行时与官方插件

- [x] 3.1 实现 `PluginManager`、运行时和贡献注册表的深模块接口，使启用返回可撤销的贡献句柄，禁用、卸载或失败时按生命周期撤销。
- [x] 3.2 实现桌宠、面板、设置、菜单、存储和权限的受控宿主能力；确保插件不能取得 HWND、GDI、SQLite 连接、任意路径、网络或动态 Tauri command。
- [x] 3.3 把现有静态皮肤清单抽取为内置资源适配器，并让桌宠窗口只从当前已启用的皮肤贡献读取经过校验的资源。
- [x] 3.4 添加核心级 `simple-cloud` 回退资源和首次启动 bootstrap；首次注册时只将 `simple-cloud` 登记为已安装皮肤，其他官方皮肤只进入可安装目录。
- [x] 3.5 将现有使用统计、快捷面板和大小设置包装为预装官方插件，并验证它们通过与本地资源插件相同的状态和贡献路径装配。
- [x] 3.6 重构启动和关闭流程，使生命周期模块只组装宿主和官方插件；确认重复启用不会创建第二个桌宠窗口、采集器、快捷面板或完整统计窗口。
- [x] 3.7 迁移现有皮肤和大小偏好：保留旧文件兼容读取，将无法映射的旧皮肤回退到 `simple-cloud`，并保证迁移失败不影响统计数据库。
- [x] 3.8 为宿主运行时和皮肤切换补充 Rust 测试，覆盖贡献启用/撤销、部分失败回滚、当前皮肤插件禁用、默认回退、位置/大小/DPI 保持和插件故障隔离。

## 4. 通用 IPC 与插件管理界面

- [x] 4.1 定义并注册固定的插件 Tauri commands，用于查询目录和已安装清单、导入包、安装、启用、禁用、卸载、读取贡献和执行批准动作；拒绝路径、脚本、URL 和窗口句柄输入。
- [x] 4.2 在 TypeScript 中增加插件 DTO、错误模型和 API 封装，确保前端只使用稳定插件 ID、贡献 ID 和动作 ID。
- [x] 4.3 将快捷面板拆分为宿主 shell 与声明式贡献渲染器，把统计摘要、大小设置和已安装皮肤选择迁移为官方贡献，禁止动态加载任意 Svelte 组件。
- [x] 4.4 新增按需打开的中文插件管理界面，展示本地官方目录、已安装/启用/禁用/故障状态、版本、来源、贡献类型和权限摘要。
- [x] 4.5 在插件管理界面中实现本地 `.petpack` 文件选择、导入预览、确认安装、启用、禁用、卸载和受保护默认皮肤的明确反馈。
- [x] 4.6 调整皮肤界面，使其只显示已安装且启用的皮肤；未安装的橙色小龙、三花猫等官方皮肤只在插件目录中显示安装入口。
- [x] 4.7 为前端补充 Vitest/Svelte 测试，覆盖首次只有默认皮肤、离线目录、合法/非法导入反馈、启用/禁用/卸载、故障详情、默认皮肤保护和关闭界面后的状态保持。

## 5. 官方目录与资源发布

- [x] 5.1 将默认云朵、橙色小龙和三花猫整理为独立官方插件包或等价的内置包源；仅默认云朵进入首次已安装清单，并继续核验素材来源和发布授权。
- [x] 5.2 创建本地官方目录数据，列出未安装官方皮肤的中文名称、版本、来源、缩略图、贡献类型和权限摘要，确保目录不依赖网络。
- [x] 5.3 为官方插件包和目录数据加入构建期或测试期校验，确认 manifest、资源大小、透明边界、缩略图和贡献 ID 一致。
- [x] 5.4 编写面向未来 Bun SDK 的协议说明和示例 `.petpack`，说明 SDK 只负责创建、校验和打包声明式资源，不作为桌宠运行时依赖。

## 6. 验证与发布准备

- [x] 6.1 运行 Rust 格式化、聚焦单元测试和完整 `cargo test --manifest-path src-tauri/Cargo.toml`，修复插件协议、注册表、安装器和宿主运行时相关回归。
- [x] 6.2 运行 `bun run check`、`bun test` 和 `bun run build`，验证插件管理界面、快捷面板贡献渲染和静态前端构建。
- [ ] 6.3 在 Windows 10/11 x64 上手工验证首次启动、离线目录、默认云朵保护、导入合法/非法包、启用/禁用/卸载、重启恢复和安装失败回滚。
- [ ] 6.4 在 Windows 手工验证中覆盖 70%–160% 大小、DPI/多显示器、屏幕边缘、锁屏或挂起恢复、空闲暂停和跨日统计；确认插件操作不改变当前皮肤、桌宠交互或统计隐私口径。
- [ ] 6.5 记录桌宠待机 CPU、私有工作集和皮肤切换峰值，确认启动只读取 manifest、面板按需创建且已安装皮肤数量不会线性增加常驻帧内存。
- [x] 6.6 运行 `openspec validate "add-plugin-system-foundation" --strict`，确认 proposal、design、三份 spec 和任务清单一致后再进入实现流程。
