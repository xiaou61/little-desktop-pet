## 1. 基础模型与依赖

- [x] 1.1 添加 Tauri 官方日志插件的 Rust/npm 依赖、桌面 capability 权限和默认日志目标，并确认 Windows 构建可以解析锁文件。
- [x] 1.2 定义 Rust 与 TypeScript 共用的诊断级别、事件、查询、运行状态、开发者模式配置和导出结果类型。
- [x] 1.3 实现结构化上下文允许列表和路径、用户名、机器名、凭据样式脱敏器，并为稳定占位符和边界输入编写 Rust 单元测试。
- [x] 1.4 实现有界内存事件 ring、级别/模块/窗口/插件/时间筛选，并覆盖容量淘汰、空查询和关联 ID 查询测试。

## 2. 持久化与异常恢复

- [x] 2.1 实现异步日志写入适配器，配置单文件约 2 MB、最多保留 5 个归档文件，并覆盖轮转和总大小上限测试。
- [x] 2.2 实现日志目录、设置文件、会话标记和最近异常文件的目录布局、原子替换和损坏文件恢复测试。
- [x] 2.3 安装全局 panic hook，生成脱敏的最近异常摘要，并测试 panic payload、backtrace 缺失和 fallback 写入路径。
- [x] 2.4 在 Win32 `window_proc` 的 panic 回退前接入诊断记录，验证记录失败时仍执行原有安全回退。
- [x] 2.5 在应用启动、正常托盘退出和异常退出路径中写入会话状态，并覆盖重启识别、正常退出不误报和强制结束标记残留测试。

## 3. 运行时接入与状态快照

- [x] 3.1 将 `DiagnosticsManager` 注册为 Tauri 状态，接入事件记录、最近事件、配置、快照、会话和导出接口。
- [x] 3.2 为生命周期、桌宠窗口、快捷面板、采集器和插件管理器增加关键开始/成功/失败/状态变化事件，并替换关键路径的孤立 `eprintln!`。
- [x] 3.3 为桌宠点击到快捷面板创建流程传递关联 ID，并测试跨前端、Tauri 和 Win32/WebView 事件的顺序与关联。
- [x] 3.4 接入前端全局异常、Promise rejection、IPC 失败和插件管理异常，统一转换为脱敏诊断事件。
- [x] 3.5 实现桌宠、快捷面板、采集器、插件和 WebView 窗口的状态 provider，验证单个 provider 失败时快照仍返回其余状态。
- [x] 3.6 审计采集器固定采样、桌宠动画和面板轮询路径，补充测试确保只记录状态变化和错误而不记录高频事件。
- [x] 3.7 增加空闲、锁屏/挂起恢复和本地日期跨日的生命周期事件测试，确认不改变现有使用统计口径。

## 4. Tauri 接口与窗口入口

- [x] 4.1 增加获取分页日志、运行状态、最近错误、开发者配置、脱敏摘要、导出和打开日志目录的 Tauri commands，并统一错误码。
- [x] 4.2 在托盘菜单增加诊断中心入口，创建固定 label 的按需窗口，并测试重复打开时复用和聚焦已有窗口。
- [x] 4.3 实现诊断中心关闭、Tauri 退出和应用重启时的订阅清理，不影响后台日志记录和桌宠运行。
- [x] 4.4 实现发布版默认关闭、Debug 环境变量兼容、持久化开发者模式和即时日志等级切换。
- [x] 4.5 将开发者模式接入诊断中心、完整统计、快捷面板和插件管理 WebView 的 DevTools 创建/打开逻辑，并明确原生桌宠不提供 WebView DevTools。

## 5. 诊断中心前端

- [x] 5.1 创建诊断中心 HTML 入口、Svelte 页面、类型化 API 和与现有窗口一致的中文视觉样式，不在桌宠启动时预创建窗口。
- [x] 5.2 实现日志列表、级别/模块/窗口/插件/时间筛选、关联 ID 展开和实时事件订阅。
- [x] 5.3 实现生命周期时间线、最近错误、桌宠/面板/采集器/插件状态快照和持久化降级提示。
- [x] 5.4 实现开发者模式和日志等级控件，展示当前模式并处理设置保存失败、无日志和窗口关闭状态。
- [x] 5.5 实现复制脱敏摘要、导出诊断包、打开日志目录、导出进行中和导出失败等交互状态。
- [x] 5.6 为日志筛选、异常上报、状态降级、摘要复制和导出确认编写 Bun/TypeScript 前端测试。

## 6. 诊断包与隐私验证

- [x] 6.1 实现固定文件清单的临时目录、`summary.md`、`logs.jsonl`、`state.json`、`environment.json` 和可选 `last-crash.txt` 生成逻辑。
- [x] 6.2 实现统一脱敏后的压缩、用户选择保存位置、临时目录清理和可选文件缺失容错，并覆盖压缩包内容测试。
- [x] 6.3 验证导出和复制摘要不包含使用统计数据库、窗口标题、文档名、截图、键盘内容、绝对路径、用户名或机器名。
- [x] 6.4 为无异常记录、日志目录不可写、部分状态 provider 失败和断网环境补充诊断包与命令错误测试。

## 7. 聚焦验证与 Windows 手工验收

- [x] 7.1 运行 Rust 诊断模块、生命周期、插件和现有回归测试，修复与日志接入相关的失败。
- [x] 7.2 运行 Bun/TypeScript 单元测试和前端构建，确认诊断窗口入口不会影响现有桌宠、快捷面板和插件管理入口。
- [x] 7.3 运行 `openspec validate "add-local-diagnostics-center" --type change --strict`，修复规范格式、场景或任务追踪问题。
- [ ] 7.4 在 Windows 10/11 x64 手工验证首次启动、托盘打开诊断中心、重复打开复用、筛选日志、切换开发者模式和打开可用 DevTools。
- [ ] 7.5 在 Windows 上手工验证快捷面板创建/关闭、插件启用失败、日志目录不可写、强制结束后重启提示、诊断包导出和脱敏内容。
- [ ] 7.6 检查常规模式的内存 ring、日志文件总大小、诊断中心按需创建和采集器高频采样，记录资源占用结果。
