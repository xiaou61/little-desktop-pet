## 1. 项目基础

- [ ] 1.1 搭建 Tauri 2 + Svelte TypeScript/Vite 项目，使用 Bun 脚本和锁文件，生产环境不运行 Bun 或 HTTP 服务。
- [ ] 1.2 添加托盘/窗口生命周期、单实例、序列化、时间、前台识别、输入状态和 SQLite 所需的最小 Rust 依赖与 Windows feature。
- [ ] 1.3 配置 Windows 10/11 x64、本地打包资源、禁止远程网络的内容安全策略，并且不静态创建统计 WebView。
- [ ] 1.4 建立 Rust 采集器、Windows 适配器、时间归属、存储、生命周期、命令模块和类型化前端数据边界。

## 2. 确定性的活动计算

- [ ] 2.1 定义平台无关的软件身份、活动快照、记录状态、待写入汇总和每日汇总模型，并测试 UI 序列化契约。
- [ ] 2.2 编写表驱动测试，覆盖活跃归属、软件切换、未知进程、自身进程排除和区间只计算一次。
- [ ] 2.3 实现时间归属状态机，满足活跃、五分钟空闲、恢复、锁屏和不可用状态场景。
- [ ] 2.4 使用假时钟测试跨午夜、时区/墙上时钟跳变、单调时长和超过十秒的采样间隔。
- [ ] 2.5 实现本地日期分配和不确定间隔排除，使全部时间边界测试通过。

## 3. Windows 采集适配器

- [ ] 3.1 使用 Windows API 以只查询权限识别前台窗口和进程，包括路径规范化和自身进程排除。
- [ ] 3.2 解析并缓存可执行文件名、Windows 文件描述/产品名；无法访问进程时返回不可识别快照，不让采集器崩溃。
- [ ] 3.3 实现输入空闲时长和活动输入桌面检测，五分钟空闲和安全桌面期间不读取输入内容且不计时。
- [ ] 3.4 实现两秒采样工作线程和控制消息循环，支持干净终止，并把十秒作为最大可归属间隔。
- [ ] 3.5 测试路径规范化、元数据回退和 API 错误映射，并在 Windows 手工验证前台切换与锁屏恢复。

## 4. SQLite 持久化

- [ ] 4.1 创建第一版 `applications` 和 `daily_usage` schema，配置外键、忙等待、WAL、同步策略和每用户应用数据路径。
- [ ] 4.2 实现事务化的软件 upsert 和每日时长累加，使用临时数据库测试新增、重复写入和多日汇总。
- [ ] 4.3 实现 30 秒缓冲、提交后清理、写入失败重试和退出时最终保存，事务失败时不得丢弃待写入数据。
- [ ] 4.4 实现每日汇总查询，恰好合并一次已提交和内存数据，计算占比、稳定排序，且不返回可执行文件路径。
- [ ] 4.5 添加数据库重开和中断会话测试，确认已提交汇总可读、不补记停止时段、只有未提交缓冲可能缺失。

## 5. Tauri 生命周期与命令

- [ ] 5.1 在 UI 创建前启动唯一采集器，创建中文托盘菜单，并将首次启动入口交给桌宠主窗口。
- [ ] 5.2 实现统计面板的按需创建/聚焦和关闭销毁，确认面板关闭后采集继续且统计 `WebviewWindow` 不再存在。
- [ ] 5.3 实现单实例激活，重复启动只聚焦已有桌宠，不创建第二个采集器或数据库写入者。
- [ ] 5.4 通过工作线程暴露类型化 `get_daily_usage` 和 `get_tracker_status` 命令，设置有限等待，并阻止 JavaScript 直接访问数据库或路径。
- [ ] 5.5 实现托盘退出和操作系统关机处理，请求最终保存并终止工作线程，不让应用卡死。

## 6. 每日统计面板

- [ ] 6.1 实现类型化 Tauri invoke 客户端、时长/占比格式化、日期帮助函数及其单元测试。
- [ ] 6.2 实现紧凑统计面板，包含今天总时长、记录状态、前一天/后一天/今天按钮、软件行、时长、占比和 CSS 进度条。
- [ ] 6.3 实现稳定的加载、空数据、不可用和保存错误状态，并且不增加图表、远程字体或复杂组件库。
- [ ] 6.4 完成约 460×620 的响应式 Windows 布局、系统明暗模式、键盘焦点、图标提示、文本溢出和未来日期禁用。
- [ ] 6.5 测试有数据/空数据、内存数据、错误状态、排序和日期切换，并截图检查桌面与最小尺寸下的重叠和裁切。

## 7. 验证与交接

- [ ] 7.1 运行 Rust 格式化、lint、单元/集成测试、Svelte 类型检查、前端测试以及开发和 release 构建，解决全部失败。
- [ ] 7.2 在 Windows 手工验证两个前台软件分别累计，并验证自身排除、五分钟空闲、锁屏、挂起、面板关闭/重开和重复启动。
- [ ] 7.3 检查 SQLite schema 和代表性数据，确认没有窗口标题、文档名、网址、输入内容、截图或前端可见路径。
- [ ] 7.4 强制终止进程并模拟数据库写入失败，验证重启恢复、有限数据损失、待写入保留和错误提示。
- [ ] 7.5 使用 release 构建在统计面板销毁后测量五分钟，记录平均 CPU 和私有工作集，满足低于 1% 和不超过 50 MiB。
- [ ] 7.6 运行 `openspec validate --all --strict`，逐条检查 daily-app-usage 场景，在实现被接受前不要归档。
